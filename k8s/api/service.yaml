apiVersion: v1
kind: Service
metadata:
  name: rahal-api
  namespace: rahal
  labels:
    app.kubernetes.io/name: rahal-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rahal-api
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
